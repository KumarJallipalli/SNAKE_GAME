/*
    PROBLEMS :
    1.) Moving the Snake
    2.) Randomly generated apple is overlapping with snake
*/

public class Main {
    public static void main(String[] args) {

        /* Instantiating the GameFrame class directly without using variable
           Since we were never going to use that variable again     */
        new GameFrame();
    }
}
